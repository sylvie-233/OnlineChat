<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.sylvie233</groupId>
        <artifactId>onlinechat</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>onlinechat-service</artifactId>
    <packaging>jar</packaging>
    <name>OnlineChat-Service</name>
    <description>业务逻辑层 — 用户/联系人/消息/群组/文件/通知等业务服务</description>

    <dependencies>
        <!-- 依赖 repository -->
        <dependency>
            <groupId>com.sylvie233</groupId>
            <artifactId>onlinechat-repository</artifactId>
        </dependency>

        <!-- Spring 事务 -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
        </dependency>

        <!-- Redis -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- MinIO 对象存储 -->
        <dependency>
            <groupId>io.minio</groupId>
            <artifactId>minio</artifactId>
            <version>8.5.12</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
